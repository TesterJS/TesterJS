TODO LIST:

* Add README
* Add project configuration:
    - editorconfig (√)
    - CI
* Create as node module (√)
* Add Unit Tests
    - Function return what is expected and sets correctly its specific configuration
    - Function return what is expected and sets the default configuration
    - Check that a test iterates N number of times as its configuration defines
* Add Coverage
* Prepare to recieve and array of tests
    - Pass array of objects with the following structure:
        - A function test${Name} the coded is going to be tested
        - An array of params
* Write in ES6
* Nice to have:
    - TDD
    - Add perfomance test to DOM manipulation

