TODO LIST:

* Add Unit Tests
    - Function return what is expected and sets correctly its specific configuration
    - Function return what is expected and sets the default configuration
    - Check that a test iterates N number of times as its configuration defines
    - Tester return the time in milliseconds
* Prepare to recieve two files with the functions to compare against each other
    - As ci tool user should provide a coople of files that he wants to compare
* Nice to have:
    - Give some improvement ideas for the functions passed
    - TDD
    - Add perfomance test to DOM manipulation

